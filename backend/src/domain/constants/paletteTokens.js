"use strict";

const PALETTE_TOKEN_KEYS = Object.freeze([
  "primary",
  "secondary",
  "accent",
  "background",
  "surface",
  "text",
  "muted",
  "border"
]);

module.exports = {
  PALETTE_TOKEN_KEYS,
};
